export default class Overworld {
  constructor(config) {
    this.element = config.element;
    this.canvas = this.element.querySelector(".game-canvas");
    // this.ctx = this.canvas.getContext("2d");
  }

  init() {
    console.log("This is the Overworld"); // Debug log
  }
}
